#include<stdio.h>
using namespace std;
int main() 
{ 
    long long r, k=0; 
    scanf ("%lld", &r); 
    for(int i=-1*r; i<=r; i++) 
    { 
        for(int j=-1*r; j<=r; j++) 
        { 
            if(i*i+j*j<=r*r) k++; 
        } 
    } 
    
    printf ("%lld", k);  
}
