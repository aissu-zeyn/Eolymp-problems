#include <stdio.h>
using namespace std; 
int main() 
{ 
    long long x, n, m, q, p; 
    scanf("%lld%lld%lld", &x, &n, &m);
    
    x = x%m; 
    q = x; 
    for (int i = 1; i < n; i++)
    {
    q = q*x%m; 
    p=q%m; 
    }
    printf ("%lld", p); 
    return 0; 
    
}
