#include <stdio.h>     
using namespace std;
int main()
{
	long long n;
	scanf("%lld", &n);
	
	if(n==0){ printf("1"); return 0; }
	
	int c=0;
	while(n>0)
	{
		n/=10;
	    c++;
	}
	printf("%d", c);
	return 0;
}
